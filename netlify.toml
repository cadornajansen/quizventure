[build]
  publish = "public"
  functions = "netlify/functions"
  command = "echo 'Static site with Supabase backend'"

# Redirects for SPA routing - redirect all 404s to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
